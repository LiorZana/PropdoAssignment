export type NavButton = { label: 'Listings'; href: '/real-estate' } | { label: 'Map'; href: '/map' };

const navButtons: NavButton[] = [
  { label: 'Listings', href: '/real-estate' },
  { label: 'Map', href: '/map' }
];

export default navButtons;
